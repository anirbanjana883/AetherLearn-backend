version: '3.8'

services:
  worker_engine:
    container_name: aether_worker_engine
    build:
      context: .
      dockerfile: Dockerfile.worker 
    env_file: .env
    volumes:
      - .:/app
      - /app/node_modules